#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
typedef string str;
#define vec vector

int main() {

    bool bptando = 1;
    while (bptando) {
        int x;
        cin >> x;

        if ( x == 0) bptando = 0;


    }

    return 0;
}
